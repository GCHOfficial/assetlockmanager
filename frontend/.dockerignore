# Ignore node_modules to avoid copying local modules into the image
node_modules

# Ignore build output directory
dist

# Ignore environment files if they exist (should be handled by Docker build args/secrets if needed)
.env* 